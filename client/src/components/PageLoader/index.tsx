import React from "react";
import { Container, CustomSpin } from "./loader.style";
const PageLoader = () => {
  return (
    <Container>
      <CustomSpin />
    </Container>
  );
};

export default PageLoader;
